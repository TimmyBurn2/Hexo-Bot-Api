description: >-
  The token is valid but lacks the required scope (e.g. `bot:play`), or the
  account is not a bot account where one is required.
content:
  application/json:
    schema:
      $ref: ../schemas/Error.yaml
    example:
      error: missing-scope
      message: This token lacks the bot:play scope.
